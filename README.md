# HarnessTax

Generated publication of the AgentBRANE harness x model benchmark dashboard.

Every file here is produced by `python -m dashboard publish-pages` from the
AgentBRANE repository; the publish force-replaces this branch, so do not edit
by hand. `.nojekyll` is required because the site ships paths that GitHub's
Jekyll pipeline would otherwise skip.
